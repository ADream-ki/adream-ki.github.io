# Release Branch

This branch contains the built static site files deployed to GitHub Pages.

## Structure
- All files are generated by the build process
- Do not edit files in this branch directly
- Changes will be overwritten by the next deployment

## Deployment
Deployed automatically by GitHub Actions when changes are pushed to:
- main branch (code changes)
- post branch (new articles)
- work branch (project updates)